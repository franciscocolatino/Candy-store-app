<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sistema de Controle - Confeitaria" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon bonito (ícone da aba do navegador) -->
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/859/859270.png">
    <link rel="apple-touch-icon" href="https://cdn-icons-png.flaticon.com/512/859/859270.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen flex flex-col">

    <%= render 'shared/navbar' %>

    <main class="flex-1">
      <%= yield %>
    </main>

    <%= render 'shared/footer' %>

  </body>
</html>
