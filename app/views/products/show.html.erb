<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-10">
      <div class="card shadow-lg rounded-4 border-0">
        <div class="card-header bg-white py-4 border-bottom">
          <div class="d-flex justify-content-between align-items-center">
            <h1 class="h4 mb-0 text-primary fw-semibold">
              <i class="fas fa-box-open me-2 text-secondary"></i><%= @product.name %>
            </h1>
            <div class="btn-group">
              <%= link_to edit_product_path(@product), class: 'btn btn-sm btn-outline-primary', title: 'Editar produto', data: { bs_toggle: 'tooltip' } do %>
                <i class="fas fa-edit me-1"></i> Editar
              <% end %>
              <%= link_to products_path, class: 'btn btn-sm btn-outline-secondary', title: 'Voltar para a lista de produtos', data: { bs_toggle: 'tooltip' } do %>
                <i class="fas fa-arrow-left me-1"></i> Voltar
              <% end %>
            </div>
          </div>
        </div>

        <div class="card-body">
          <div class="row g-4">
            <!-- Coluna Informações -->
            <div class="col-md-6">
              <h5 class="text-muted mb-3">📋 Informações do Produto</h5>
              <span class="badge bg-info text-dark fs-6 mb-2 px-3 py-2"><%= @product.category %></span>
              <p class="lead text-dark mt-2"><%= @product.description %></p>

              <div class="mt-4">
                <h6 class="text-muted">💰 Preço</h6>
                <span class="display-6 text-success fw-bold"><%= number_to_currency(@product.price) %></span>
              </div>

              <hr class="my-4">

              <h5 class="text-muted mb-3">📦 Detalhes do Estoque</h5>
              <div class="bg-light p-4 rounded shadow-sm">
                <div class="d-flex justify-content-between align-items-center mb-3">
                  <div>
                    <h6 class="mb-1 fw-semibold">Estoque Total</h6>
                    <span class="h4 text-dark"><%= @product.lots.sum(:quantity) %></span> unidades
                  </div>
                  <%= link_to inventory_product_path(@product), class: 'btn btn-sm btn-primary' do %>
                    <i class="fas fa-boxes me-1"></i> Gerenciar
                  <% end %>
                </div>

                <% if @product.lots.any? %>
                  <h6 class="text-uppercase text-muted small mb-2">Lotes disponíveis</h6>
                  <div class="list-group list-group-flush">
                    <% @product.lots.order(created_at: :desc).limit(3).each do |lot| %>
                      <div class="list-group-item py-3">
                        <div class="d-flex justify-content-between">
                          <strong><%= lot.quantity %> unidades</strong>
                          <small class="text-muted"><%= l(lot.created_at, format: :short) %></small>
                        </div>
                        <div class="d-flex small text-muted mt-2">
                          <span class="me-3">
                            <i class="fas fa-industry me-1 text-secondary"></i>
                            Fabricação: <%= l(lot.manufacturing_date, format: :short) if lot.manufacturing_date %>
                          </span>
                          <span>
                            <i class="fas fa-calendar-check me-1 text-success"></i>
                            Validade: <%= l(lot.expiration_date, format: :short) if lot.expiration_date %>
                          </span>
                        </div>
                      </div>
                    <% end %>
                  </div>
                <% else %>
                  <div class="alert alert-warning mt-3 d-flex align-items-center">
                    <i class="fas fa-exclamation-triangle me-2 text-warning"></i>
                    Nenhum lote cadastrado para este produto.
                  </div>
                <% end %>
              </div>
            </div>

            <!-- Coluna Imagem -->
            <div class="col-md-6 d-flex align-items-stretch">
              <div class="card w-100 bg-white border-0 shadow-sm">
                <div class="card-body d-flex flex-column justify-content-center align-items-center text-center py-5">
                  <i class="fas fa-image fa-5x text-muted mb-4"></i>
                  <p class="text-muted fs-6">Imagem do produto</p>
                  <small class="text-muted">Funcionalidade futura de upload</small>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Rodapé -->
        <div class="card-footer bg-white border-top py-3">
          <div class="d-flex justify-content-between small text-muted">
            <div>
              <i class="far fa-calendar-plus me-1 text-primary"></i>
              Criado em: <%= l(@product.created_at, format: :long) %>
            </div>
            <div>
              <i class="far fa-edit me-1 text-success"></i>
              Atualizado em: <%= l(@product.updated_at, format: :long) %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
