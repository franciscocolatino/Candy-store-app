<div class="max-w-7xl mx-auto py-10 px-4">
  <div class="bg-white shadow-xl rounded-2xl overflow-hidden">
    <div class="px-6 py-5 border-b border-gray-200 flex justify-between items-center">
      <h1 class="text-2xl font-semibold text-gray-800 flex items-center"> <%= @product.name %> </h1>
      <div class="flex space-x-2">
        <% if @current_user.is_admin? %>  

          <%= link_to "Editar", edit_product_path(@product), 
              class: 'text-sm px-4 py-2 border border-indigo-500 text-indigo-600 rounded hover:bg-indigo-600 hover:text-white transition', 
              title: 'Editar produto'
            %>
        <% end %>
          <%= link_to "Voltar", products_path, 
          class: 'text-sm px-4 py-2 border border-gray-400 text-gray-700 rounded hover:bg-gray-700 hover:border-gray-700 hover:text-white transition', 
          title: 'Voltar para a lista de produtos'
        %>
      </div>
    </div>

    <div class="px-6 py-8 grid grid-cols-1 md:grid-cols-2 gap-8">

      <div>
        <h2 class="text-gray-600 text-lg mb-3">📋 Informações do Produto</h2>
        <span class="inline-block bg-red-100 text-red-800 text-sm px-3 py-1 rounded-full mb-4"><%= @product.category %></span>
        <p class="text-gray-800 text-base mb-6"><%= @product.description %></p>

        <div class="mb-6">
          <h3 class="text-gray-500 text-sm">💰 Preço</h3>
          <div class="text-2xl font-bold text-gray-800 mt-1"><%= number_to_currency(@product.price, unit: "R$", separator: ",", delimiter: ".", format: "%u %n") %></div>
        </div>

        <hr class="border-t border-gray-200 my-6">

        <!-- Estoque -->
        <h2 class="text-gray-600 text-lg mb-3">📦 Detalhes do Estoque</h2>
        <div class="bg-gray-50 p-4 rounded-lg shadow">
          <div class="flex justify-between items-center mb-4">
            <div>
              <p class="text-sm text-gray-500 mb-1">Estoque Total</p>
              <div class="text-xl font-semibold text-gray-800"><%= @product.lots.sum(:quantity) %> unidades</div>
            </div>
              <% if @current_user.is_admin? %>  
                <%= link_to "Gerenciar", product_lots_path(@product), class: 'bg-indigo-600 text-white text-sm px-4 py-2 rounded hover:bg-indigo-700 transition' %>
              <% end %>
            </div>

          <% if @product.lots.any? %>
            <p class="text-xs uppercase text-gray-400 mb-2">Lotes disponíveis</p>
            <ul class="space-y-4">
             
            </ul>
          <% else %>
            <div class="bg-yellow-100 text-yellow-800 text-sm p-4 rounded flex items-center mt-4">Nenhum lote cadastrado para este produto.</div>
          <% end %>
        </div>
      </div>

      <div class="flex items-center justify-center">
        <div class="border border-gray-200 bg-white rounded-lg shadow w-full flex flex-col items-center justify-center py-20">
          <p class="text-sm text-gray-500">Imagem do produto</p>
          <span class="text-xs text-gray-400">Funcionalidade futura de upload</span>
        </div>
      </div>
    </div>

    <!-- Rodapé -->
    <div class="bg-gray-50 border-t border-gray-200 px-6 py-4 text-sm text-gray-500 flex justify-between">
      <div>Criado em: <%= l(@product.created_at, format: :long) %></div>
      <div>Atualizado em: <%= l(@product.updated_at, format: :long) %></div>
    </div>
  </div>
</div>
